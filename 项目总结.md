# 基于 Open WebUI 的企业级知识库管理后台 — 项目总结

> 开发者：顾扬 | 2026-07-20 ~ 2026-08-14
> 基于 [Open WebUI](https://github.com/open-webui/open-webui) (128K+ Star) 二次开发
> GitHub: https://github.com/yangyang-qaq/enterprise-knowledge-dashboard

---

## 一、项目概述

在 Open WebUI 的 FastAPI + SvelteKit 架构上进行二次开发，独立设计并实现了企业级 RAG 知识库管理仪表板，涵盖 12 个 Phase（8 个功能模块 + 4 个质量保障），新增 39 个 API 端点，12 张数据库表，8 个前端 Tab。

---

## 二、功能模块

### Phase 1-2：RAG 可观测性

| 模块 | 功能 | 核心价值 |
|---|---|---|
| 🧩 分块管理 | 预览/合并/拆分/重建向量 | 可视化 RAG 分块结果，手动修正提升检索准确率 |
| ⏳ 进度监控 | SSE 实时推送 + 轮询兜底 | 文档处理不再黑盒 |

### Phase 3-4：RAG 质量管理

| 模块 | 功能 | 核心价值 |
|---|---|---|
| 📊 检索评估 | 查询→Top-K→标注→Recall/Precision/MRR | 把 RAG 从黑盒变成可度量系统 |
| 📸 版本管理 | 快照/回滚/差异对比 | 知识库版本可控可回溯 |

### Phase 5-6：RAG 增强

| 模块 | 功能 | 核心价值 |
|---|---|---|
| 📝 Prompt 配置 | 模板编辑/变量替换/聊天管道注入 | Prompt Engineering 融入 RAG 管道 |
| 🔪 多策略分块 | 7 种算法 + 关键词提取 + 问题生成 | 按文档类型自适应分块 |

### Phase 7-8：Agent 协作

| 模块 | 功能 | 核心价值 |
|---|---|---|
| 🤖 Agent 工作流 | 检索→分析→汇报→校验，真实 LLM 调用，SSE 流式，Word 报告下载 | 多 Agent 协作编排，检索溯源，结果导出 |

### Phase 9-10：质量保障

| 模块 | 功能 | 核心价值 |
|---|---|---|
| 🧪 单元测试 | 44 个 pytest 用例覆盖 7 种分块策略（另有 39 个图谱检索用例） | 核心逻辑可回归 |
| 🔥 并发压测 | Locust 100 并发 4 场景 + Mock LLM + PostgreSQL 多 worker | 定位生产并发瓶颈，暴露 ChromaDB 写锁 |

### Phase 11：生成质量门禁

| 模块 | 功能 | 核心价值 |
|---|---|---|
| 🎯 Faithfulness 评测 | 40 条黄金集 + LLM-as-judge 忠实度判定（两档：judge-only/full） | 生成幻觉可度量，首次运行即发现向量未入库 bug |
| 🚦 CI 门禁 | GitHub Actions 每个 PR 自动跑，低于门槛 build 失败 | 质量回归自动化 |

### Phase 12：跨文档召回（知识图谱增强 RAG）

| 模块 | 功能 | 核心价值 |
|---|---|---|
| 🕸️ 知识图谱扩展召回 | LLM 抽「实体—关系—实体」三元组（5 张表）→ 多跳游走 → 与基础召回融合 | 每次查询多召回 2 个关联 chunk；实测定位并消除了「图谱挤占基础召回」的负收益 |
| 📊 召回 A/B 评测 | 文件级 recall@K，免人工标注，跨文档/单文档分层报告 + 回归门 | 首次运行即暴露自己的 ground truth 是循环论证；ground truth 加机械校验 |

> **这个 Phase 的结论是诚实的中性偏保守的**：图谱把召回池扩大了 2 个 chunk/query，但**不改变 top-K 的排序**——全量/跨文档/单文档三个档位的 Δ 都是 +0.0000，没有任何参数配置能让跨文档 Δ 转正。第一版默认（`rerank` 融合 + 0.05 先验）甚至让结果**变差**（全量 −0.0730、跨文档 −0.1458），根因是先验按 logit 尺度标定却打在了 sigmoid 分数上。改成 `append` 融合后负收益归零而图谱贡献保留。

---

## 三、技术统计

| 指标 | 数量 |
|---|---|
| 新增数据库表 | 12 张 |
| 新增 API 端点 | 42 个 |
| 分块策略 | 7 种 |
| Agent 角色预设 | 5 种 |
| 新增前端页面 | 7 个 Tab |
| 新增前端组件 | 12 个 |
| Phase 12 改动文件 | 33 个（18 改 + 15 新增，不含镜像副本） |
| 排查解决问题 | 41 个 |
| 上游 PR | 2 个 |
| 单元测试用例 | 83 个（分块策略 44 + 图谱检索 39） |
| 压测场景 | 4 个（读/聊天/上传/混合） |
| 离线评测 | Faithfulness 40 条黄金集 + 召回 A/B 16 条 + 2 档 CI 门禁 + 图谱回归门 |
| 代码行数 | ~8000 行（Phase 12 新增约 4100 行） |

---

## 四、技术栈

```
后端：Python / FastAPI / SQLAlchemy / ChromaDB / LangChain / httpx
前端：SvelteKit / TypeScript / Tailwind CSS
AI：DeepSeek API / RAG / Prompt Engineering / SSE
数据库：SQLite（开发）/ PostgreSQL（生产）+ Alembic 迁移
测试：pytest / Locust 并发压测 / uvicorn 多 worker
```

---

## 五、技术亮点

1. **SQLite UNIQUE 约束冲突**：深入排查 3 种失败方案，最终用 `+10000 偏移 + 重新编号` 解决 chunk_index 中间态冲突
2. **双通道可靠性**：SSE 实时推送 + 3 秒轮询兜底，保证进度监控连接可靠性
3. **Prompt 闭环**：从模板编辑到聊天管道注入完整链路打通
4. **Agent 工作流真实 LLM 调用**：检索员调 query_collection + 分析员/汇报员调 DeepSeek API，变量在步骤间传递，检索结果带文档名+原文溯源
5. **`git checkout` 引发的连锁还原**：linter 干扰下多次修复→还原循环，最终采用追加式修改策略避免冲突
6. **FastAPI 路由匹配陷阱**：`/{id}` 先于 `/_workflows` 匹配导致 404，通过路由顺序调整解决
7. **测试体系落地**：为分块引擎编写 44 个 pytest 单元测试全绿（另有 39 个图谱检索用例，合计 83 个），并沉淀 Claude Code 单元测试技能（pytest + vitest 双框架、自动生成报告）
8. **并发压测定位生产瓶颈**：Locust 模拟 100 并发 + Mock 外部 LLM/Embedding + PostgreSQL 多 worker 模拟生产，定位出 ChromaDB 多进程并发写损坏 HNSW 索引（`KB.file_add` 51.92% 失败）这一核心瓶颈，切远程 Chroma 后复测 `KB.file_add` 失败率归零，完成「发现→定位→修复→验证」闭环
9. **内存可视化诊断**：通过 Windows 提交限制（commit limit）发现内嵌 Chroma 在多 worker 下吃满 ~54GB 虚拟内存，从 73MB 释放到 54GB，量化了 PersistentClient 多进程部署的内存放大问题
10. **LLM-as-judge 生成质量门禁**：搭建 RAG Faithfulness 离线评测（40 条黄金集 + 拆原子主张逐条判定），接入 GitHub Actions 每个 PR 自动跑、低于门槛即 build 失败；首次运行即定位出「文件 status=completed 但向量未入库」的真实 bug，体现 full 档真实检索的价值
11. **知识图谱扩展召回**：LLM 抽「实体—关系—实体」三元组建图（5 张表），检索时从种子 chunk 的实体多跳游走回捞关联 chunk，让「答案要跨文档拼」的问题能召回另一份文档。关键收获是**负结果的处理**——第一版融合策略实测让文件级召回变差（全量 Δ −0.0730、跨文档 Δ −0.1458），定位到根因是 +0.05 先验按 logit 尺度标定却打在了 sigmoid 分数上（不是 tiebreak 而是强行提拔），改 `append` 融合后负收益归零而召回增量保留
12. **评测工具本身也要被评测**：文件级召回 A/B 第一次跑就暴露自己的 ground truth 是循环论证——「跨文档」档用检索输出定义，导致 40/40 条全部入选、该档位毫无区分度。改成手写 ground truth + 机械校验（每条证据必须是声明文件里的原文子串）后才可信。这类错误不会报错，只会让数字好看

---

## 六、上游贡献

| PR | 内容 |
|---|---|
| [#27222](https://github.com/open-webui/open-webui/pull/27222) | fix: knowledge_fs grep splits on literal backslash-n |
| [#27249](https://github.com/open-webui/open-webui/pull/27249) | fix: mutable default argument in generate_function_chat_completion |

---

## 七、简历项目描述

**项目名称**：基于 Open WebUI 的企业级知识库管理后台

**项目描述**：在开源项目 Open WebUI（128K+ Star）的 FastAPI + SvelteKit 架构上进行二次开发，独立设计并实现了可视化的 RAG 知识库管理仪表板，涵盖文档分块管理、SSE 进度监控、检索质量评估（Recall/Precision/MRR）、版本快照回滚、Prompt 模板配置、多策略文档分块、多 Agent 工作流编排、知识图谱扩展召回。深度参与 RAG 全链路优化与 Agent 协作系统设计。

**技术栈**：Python / FastAPI / SQLAlchemy / ChromaDB / LangChain / SvelteKit / TypeScript / Tailwind CSS / DeepSeek API / SSE / Prompt Engineering / Agent Workflow

**主要工作**：
- 设计 12 张新数据库表 + Alembic 迁移，遵循 SQLAlchemy 异步架构，兼容 SQLite/PostgreSQL
- 新增 39 个 RESTful API 端点，复用 JWT 认证和 RBAC 权限体系
- 集成 7 种分块策略 + jieba 关键词提取 + 问题自动生成
- 实现 Prompt 模板配置并接入聊天管道，支持变量替换
- 设计多 Agent 工作流编排系统，5 种角色预设，真实 LLM 调用，检索溯源
- 设计检索质量评估流程：标注→Recall@K/Precision@K/MRR 自动计算
- 使用 SSE + 轮询双通道实现实时推送
- 搭建 Locust 并发压测体系（Mock LLM/Embedding + PostgreSQL + 多 worker uvicorn），模拟 100 并发定位出 ChromaDB 多进程并发写损坏 HNSW 索引的瓶颈，并切远程 Chroma 验证修复有效（`KB.file_add` 失败率归零）
- 搭建 RAG Faithfulness 离线评测体系（40 条黄金集 + LLM-as-judge 忠实度判定 + GitHub Actions CI 门禁，低于门槛自动 build 失败），首次运行即发现并修复「文件处理状态 completed 但向量未入库」的真实 bug
- 实现知识图谱增强 RAG：用 LLM 从 chunk 抽「实体—关系—实体」三元组建图（5 张表、807 实体/534 边），检索时多跳游走回捞关联 chunk 与基础召回融合，让「答案要跨文档拼」的问题能召回另一份文档
- 搭建文件级召回 A/B 评测（16 题手写 ground truth + 机械校验 + 回归门，免人工标注）：首次运行即暴露自己的 ground truth 是循环论证；实测定位并修复了图谱融合策略的负收益（跨文档 recall Δ 从 −0.1458 转为 0.0000）
- 排查并解决 41 个技术问题（SQLite 约束/路由冲突/环境加载/变量传递/并发写锁/向量未入库/分数量纲不一致等）
- 向 Open WebUI 上游提交 2 个 bug fix PR
- 为分块引擎与图谱检索编写 83 个 pytest 单元测试（分块策略 44 + 图谱检索 39），并沉淀 Claude Code 单元测试技能
